<h1>Resources</h1>
<!-- eventually make filterable! Like in that one exercise-->
<%= link_to "Add a resource", new_resource_path %>
<ul>
  <% @resources.each do |r| %>
    <li><%= link_to r.full_title, resource_path(r) %></li>
  <% end %>
</ul>
